import { DroppableZoneDirective } from './droppable-zone.directive';

describe('DroppableZoneDirective', () => {
  it('should create an instance', () => {
    const directive = new DroppableZoneDirective();
    expect(directive).toBeTruthy();
  });
});
